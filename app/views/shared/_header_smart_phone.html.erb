 <div data-role="header" data-theme="a">
    <% if controller.action_name == "index" || controller.action_name == "setting" %>
    <a href="." data-icon="home" data-iconpos="notext">Home</a>
    <% else %>
    <a data-icon="back" id="back" data-iconpos="notext">Home</a>
    <% end %>
    <h1><%= @title %></h1>
    <a data-icon="refresh" id="refresh" data-iconpos="notext" class="ui-btn-right">refresh</a>
  </div><!-- /header -->
